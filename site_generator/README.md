# Astro Site Generator

Автоматический генератор сайтов на базе Astro из JSON-конфигурации. Система использует LLM для создания полноценных одностраничных сайтов с современным дизайном, тематическими изображениями и адаптивной версткой.

## 🚀 Возможности

- **Автоматическая генерация** Astro компонентов из JSON
- **Тематические изображения** - автоматический подбор изображений на основе контекста бизнеса
- **Современный дизайн** - Premium UI с Tailwind CSS, glassmorphism эффектами
- **Адаптивная верстка** - Полностью responsive дизайн
- **Русский язык** - Все тексты генерируются на русском языке
- **Логотипы** - Поддержка загрузки логотипов
- **Контактная информация** - Автоматическое отображение контактов из JSON

## 📋 Требования

- Python 3.10+
- API ключ для OpenRouter (или другой совместимый API)

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd MVP
```

2. Установите зависимости:
```bash
pip install langchain-openai langgraph
```

3. Настройте API ключ в `main.py`:
```python
API_KEY = "your-api-key-here"
BASE_URL = "https://openrouter.ai/api/v1"
```

## 📖 Использование

### Базовое использование

```python
from main import generate_site

# Ваш JSON с данными для сайта
json_data = {
    "project": {
        "business": {
            "name": "лысинка",
            "industry": "услуги",
            # ... остальные данные
        },
        # ... остальная структура
    }
}

# Генерация сайта
result = generate_site(json_data)

# result содержит:
# {
#     "files": [
#         {"name": "package.json", "content": "..."},
#         {"name": "src/pages/index.astro", "content": "..."},
#         # ... остальные файлы
#     ]
# }
```

### Использование с кастомными параметрами

```python
from main import generate_site

result = generate_site(
    json_data,
    api_key="your-custom-api-key",
    base_url="https://custom-api-url.com/v1"
)
```

### Использование класса напрямую

```python
from main import SiteGenerator

# Создание генератора
generator = SiteGenerator(
    api_key="your-api-key",
    base_url="https://openrouter.ai/api/v1"
)

# Генерация сайта
result = generator.generate(json_data)
```

## 📁 Структура JSON

JSON должен содержать следующую структуру:

```json
{
  "project": {
    "client": {
      "name": "",
      "phone": "+79998147243",
      "email": "",
      "telegram id": "7052064142",
      "preferred_contact": "telegram"
    },
    "business": {
      "name": "лысинка",
      "type": "визитка",
      "industry": "услуги",
      "location": "Москва, улица геолокации 11",
      "goal": "предоставляю стрижку на лысину"
    }
  },
  "design": {
    "colors": {
      "primary": "#8B7A5D",
      "secondary": "#9C917B",
      "accent": "#2D2112",
      "background": "#ffffff",
      "text": "#62575B"
    },
    "fonts": {
      "heading": "Inter",
      "body": "Inter"
    },
    "images": {
      "hero": {
        "url": "",
        "alt": ""
      },
      "about": {
        "url": "",
        "alt": ""
      },
      "gallery": [
        {
          "url": "https://...",
          "name": "Image 1",
          "alt": "Image 1"
        }
      ],
      "logo": {
        "url": "",
        "width": "200px"
      }
    }
  },
  "content": {
    "hero": {
      "headline": "Стрижка на лысину с искусством",
      "subheadline": "Ваш стиль — наша забота",
      "cta_text": "Связаться",
      "cta_url": "#contacts"
    },
    "sections": [],
    "features": [],
    "contacts": {
      "phone": "+79998147243",
      "email": "",
      "telegram": "",
      "address": "",
      "work_hours": ""
    }
  },
  "structure": {
    "navigation": [],
    "footer": {
      "links": [],
      "social": {
        "telegram": "",
        "whatsapp": "",
        "vk": "",
        "instagram": ""
      },
      "copyright": "© 2025 лысинка"
    }
  }
}
```

## 🎨 Генерируемые компоненты

Система автоматически создает следующие компоненты:

1. **Navigation** - Навигационное меню с логотипом
2. **Hero** - Главная секция с заголовком и изображением
3. **Features** - Секция с особенностями/услугами
4. **About** - Секция "О нас" с описанием
5. **Gallery** - Галерея изображений
6. **Contact** - Контактная информация и футер

## 🖼️ Изображения

### Автоматический подбор изображений

Если в JSON не указаны URL изображений, система автоматически:
- Анализирует контекст бизнеса (название, индустрия, описание)
- Генерирует тематические поисковые запросы
- Подбирает подходящие изображения из Picsum Photos

### Поддержка логотипов

Логотип можно указать в `design.images.logo`:
- Если `url` указан - используется изображение логотипа
- Если `url` пустой - генерируется текстовый логотип с градиентом

## 🎯 Особенности генерации

### Умная генерация текста
- Исключает технические детали из JSON
- Создает естественный маркетинговый текст
- Использует реальные адреса из данных
- Фокусируется на качестве и премиум-опыте

### Контрастность цветов
- Автоматическая проверка контрастности
- Темный текст на светлых фонах
- Светлый текст на темных фонах
- Правильные цвета для карточек и секций

### Кликабельные ссылки
- Телефон: `tel:` ссылки
- Email: `mailto:` ссылки
- Telegram: `https://t.me/` ссылки
- Социальные сети: кликабельные иконки

## 📦 Структура выходных файлов

Генератор создает полную структуру Astro проекта:

```
├── package.json
├── astro.config.mjs
├── tailwind.config.mjs
├── src/
│   ├── env.d.ts
│   ├── styles/
│   │   └── global.css
│   ├── layouts/
│   │   └── Base.astro
│   ├── components/
│   │   ├── Navigation.astro
│   │   ├── Hero.astro
│   │   ├── Features.astro
│   │   ├── About.astro
│   │   ├── Gallery.astro
│   │   └── Contact.astro
│   └── pages/
│       └── index.astro
└── public/
    ├── favicon.svg
    └── robots.txt
```

## 🔄 Workflow генерации

Процесс генерации проходит через следующие этапы:

1. **Parse CTM** - Парсинг данных клиента
2. **Scaffold** - Создание базовой структуры проекта
3. **Assets** - Генерация ресурсов (favicon и т.д.)
4. **Styles** - Генерация CSS с Tailwind
5. **Layout** - Создание базового layout
6. **Components** - Генерация всех компонентов
7. **Pages** - Сборка главной страницы
8. **Finalizer** - Финальная обработка

## 🛠️ API

### `generate_site(json_data, api_key=None, base_url=None)`

Основная функция для генерации сайта.

**Параметры:**
- `json_data` (Dict[str, Any]): JSON данные для генерации сайта
- `api_key` (str, optional): API ключ для LLM. Если не указан, используется дефолтный
- `base_url` (str, optional): Base URL для LLM API. Если не указан, используется дефолтный

**Возвращает:**
- `Dict[str, Any]`: Словарь с ключом `"files"`, содержащий список файлов:
  ```python
  {
      "files": [
          {"name": "package.json", "content": "..."},
          {"name": "src/pages/index.astro", "content": "..."},
          # ...
      ]
  }
  ```

### `SiteGenerator`

Класс для генерации сайтов.

**Методы:**

- `__init__(api_key=None, base_url=None)` - Инициализация генератора
- `generate(input_data)` - Генерация сайта из JSON данных

## 📝 Примеры

### Пример 1: Базовое использование

```python
from main import generate_site

json_data = {
    "project": {
        "business": {
            "name": "Мой бизнес",
            "industry": "услуги"
        }
    },
    "content": {
        "hero": {
            "headline": "Добро пожаловать",
            "subheadline": "Мы делаем отличную работу"
        }
    }
}

result = generate_site(json_data)
print(f"Сгенерировано файлов: {len(result['files'])}")
```

### Пример 2: Сохранение результата

```python
import json
from main import generate_site

result = generate_site(json_data)

# Сохранение в файл
with open("generated_site.json", "w", encoding="utf-8") as f:
    json.dump(result, f, indent=2, ensure_ascii=False)
```

### Пример 3: Использование в веб-приложении

```python
from flask import Flask, request, jsonify
from main import generate_site

app = Flask(__name__)

@app.route('/generate', methods=['POST'])
def generate():
    json_data = request.json
    result = generate_site(json_data)
    return jsonify(result)
```

## ⚙️ Конфигурация

### Настройка LLM

В файле `main.py`` можно настроить параметры LLM:

```python
API_KEY = "your-api-key"
BASE_URL = "https://openrouter.ai/api/v1"

# В классе SiteGenerator:
self.llm_coder = ChatOpenAI(
    model="anthropic/claude-3.5-sonnet",
    temperature=0.25,
    max_tokens=4000
)
```

### Настройка цветов

Цвета настраиваются через JSON:

```json
{
  "design": {
    "colors": {
      "primary": "#8B7A5D",
      "secondary": "#9C917B",
      "accent": "#2D2112"
    }
  }
}
```

## 🐛 Решение проблем

### Ошибка: "IndexError: list index out of range"

Убедитесь, что в JSON есть необходимые секции. Если `sections` пустой, система автоматически создаст пустой объект.

### Ошибка: Изображения не загружаются

Система использует Picsum Photos как fallback. Если изображения не загружаются, проверьте интернет-соединение.

### Ошибка: LLM не отвечает

Проверьте:
- Правильность API ключа
- Доступность API endpoint
- Лимиты API

## 📄 Лицензия

[Укажите лицензию]

## 👥 Авторы

[Укажите авторов]

## 🙏 Благодарности

- [LangChain](https://www.langchain.com/) - для работы с LLM
- [Astro](https://astro.build/) - за отличный фреймворк
- [Tailwind CSS](https://tailwindcss.com/) - за утилиты стилей

