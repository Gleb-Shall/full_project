#!/bin/bash
# Быстрый тест - запускает локальный сервер для проверки сайта

# Получаем директорию скрипта и переходим в корень проекта
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PROJECT_ROOT="$( cd "$SCRIPT_DIR/.." && pwd )"
cd "$PROJECT_ROOT"

echo "🚀 Быстрый тест - запуск локального сервера"
echo ""
echo "Этот скрипт создаст тестовый сайт и запустит локальный сервер"
echo "для проверки в браузере."
echo ""

# Активируем venv если есть
if [ -d "venv" ]; then
    source venv/bin/activate
fi

# Запускаем тестовый скрипт
python3 scripts/test_local_server.py

